{
  "name": "docker-info-dashboard",
  "version": "1.0.0",
  "description": "Docker container and host information dashboard via socket proxy",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "undici": "8.5.0",
    "express": "^5.0.0",
    "axios": "^1.16.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
